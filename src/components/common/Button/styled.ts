import styled from "@emotion/styled";

export const ButtonElement = styled.button`
  width: 100%;
  height: 5rem;

  font-family: "Pretnedard Variable";
  font-size: 1.6rem;
  font-weight: 600;
  text-align: center;
  color: #fff;

  margin: auto auto 7rem auto;

  border: none;
  border-radius: 0.9rem;
  background-color: #0a0a0a;
`;
